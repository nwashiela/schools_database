<style>
   table {
  font-family: arial, sans-serif;
  border-collapse: collapse;
  width: 70%;
  margin-left: auto;
 margin-right: auto;
 margin-top: 120px;
}

td, th {
  border: 1px solid rgba(23, 151, 180, 0.763);
  text-align: left;
  padding: 8px;
  width: 50%;
}

tr:nth-child(even) {
  background-color: rgba(23, 151, 180, 0.763);
}  
</style>

 <div>
        <div class="header">
            <h1>Schools DataBAse</h1>
        </div>

        
          <form action="/multi_subject" method="GET">
                <table>
            <tr>
              <th>id</th>
              <th>First Name</th>
              <th>Last Name</th>
              <th>Email</th>
              <th>count</th>
              <th>action</th>
             
            </tr>
            <tr>
              <td>{{this.first_name}}</td>
              <td>{{this.last_name}}</td>
              <td>{{this.email}}</td>
              <td>{{this.count}}</td>
            </tr>
              </table>
          </form>

            </form>
       

    </div> 